name: Skill Tracker Tests

on:
  push:
    branches: [main]
    paths:
      - 'src/skill_tracker.py'
      - 'tests/test_skill_tracker.py'
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install pytest
        run: pip install pytest

      - name: Copy skill_tracker.py to test root for import
        run: cp src/skill_tracker.py .

      - name: Run Skill Tracker tests
        run: python -m pytest tests/test_skill_tracker.py -v --tb=short

      - name: Verify line count (must be 400+)
        run: |
          lines=$(wc -l < src/skill_tracker.py)
          echo "skill_tracker.py has $lines lines"
          [ "$lines" -ge 400 ] || (echo "FAIL: skill_tracker.py must have 400+ lines" && exit 1)
